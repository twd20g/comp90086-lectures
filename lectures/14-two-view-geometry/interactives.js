/* No lecture-specific interactives yet. The planned components are marked
   with .todo placeholders in slides.html — see NARRATIVE.md. */

/* Components are spliced in by the shell; the lecture names the slide key. */
INIT.la = initLineAlgebra;

INIT.ph = initPlaneHomography;

INIT.ef = initEpipolarFrames;

INIT.el = initEpipolarLines;
INIT.er = initEpipolarRotate;
INIT.ev = initEpipolarLive;
